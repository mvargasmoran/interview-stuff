/*
 * This problem was asked by Dropbox.
 *
 * Create an algorithm to efficiently compute the approximate median of a
 * list of numbers.
 *
 * More precisely, given an unordered list of N numbers, find an element whose
 * rank is between N / 4 and 3 * N / 4, with a high level of certainty,
 * in less than O(N) time.
 *
 */

let unorderedList = [9,8,7,6,5,4,3,2,1,];
const median = (array) => {

};

module.exports = median;
