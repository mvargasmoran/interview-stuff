const largestSubRectangle = require('./largest-rectangle-in-matrix.js');
it('finds the area of the largest sub rectangle inside a matrix', () => {
 // expect(largestSubRectangle(4matrix)).toBe(4);
});
